# ---------------------------------------------------------
# [1] 공통 라이브러리 (PyPI 기본 저장소 사용)
# ---------------------------------------------------------
# -r 옵션을 사용하여 common 파일에 있는 pandas, numpy 등을 먼저 읽어옵니다.
-r requirements-common.txt

# ---------------------------------------------------------
# [2] PyTorch 관련 (GCP VM GPU 전용 저장소 지정)
# ---------------------------------------------------------
# --extra-index-url을 사용해야 기본 PyPI와 PyTorch 저장소를 동시에 탐색합니다.
--extra-index-url https://download.pytorch.org/whl/cu124

torch==2.6.0
torchvision==0.21.0
torchaudio==2.6.0

# ---------------------------------------------------------
# [3] 리눅스 환경 전용 추가 패키지
# ---------------------------------------------------------
# 한글 워드(HWP) 처리를 위한 라이브러리
pyhwp